{
  "name": "klucode",
  "private": true,
  "description": "Convenience wrapper: the actual project lives in web/. These scripts exist so `npm run dev` works from the repo root too.",
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "start": "npm --prefix web run start",
    "install:web": "npm --prefix web install"
  }
}
